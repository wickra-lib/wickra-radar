# On Windows the C ABI DLL is linked directly (mingw resolves the import stubs
# from wickra_radar.dll); the loader finds it via PATH at run time.
PKG_CPPFLAGS = -I$(WKRADAR_INC)
PKG_LIBS = -L$(WKRADAR_LIB) -l:wickra_radar.dll
