# Build and run the C and C++ examples against the wickra-radar C ABI.
#
#   cargo build --release -p wickra-radar-c
#   cmake -S examples/c -B examples/c/build
#   cmake --build examples/c/build --config Release
#   ctest --test-dir examples/c/build -C Release --output-on-failure
#
# WICKRA_RADAR_LIB_DIR defaults to the workspace target/release directory and can
# be overridden for out-of-tree builds.

cmake_minimum_required(VERSION 3.15)
project(wickra_radar_examples C CXX)

set(CMAKE_C_STANDARD 11)
set(CMAKE_C_STANDARD_REQUIRED ON)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

set(WICKRA_RADAR_LIB_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../../target/release"
    CACHE PATH "Directory containing the built wickra-radar C ABI library")

set(WICKRA_RADAR_INCLUDE "${CMAKE_CURRENT_SOURCE_DIR}/../../bindings/c/include")

find_library(WICKRA_RADAR_LIB
    NAMES wickra_radar libwickra_radar
    HINTS "${WICKRA_RADAR_LIB_DIR}"
    REQUIRED)

add_executable(scan scan.c)
target_include_directories(scan PRIVATE "${WICKRA_RADAR_INCLUDE}")
target_link_libraries(scan PRIVATE "${WICKRA_RADAR_LIB}")

add_executable(scanpp scan.cpp)
target_include_directories(scanpp PRIVATE "${WICKRA_RADAR_INCLUDE}")
target_link_libraries(scanpp PRIVATE "${WICKRA_RADAR_LIB}")

# The golden corpus, globbed at configure time and written into a header, so the
# test covers a spec added to the corpus without editing the test.
# CONFIGURE_DEPENDS makes the build system re-glob when the directory changes.
set(GOLDEN_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../../golden")
file(GLOB GOLDEN_SPEC_FILES CONFIGURE_DEPENDS "${GOLDEN_DIR}/specs/*.json")
set(GOLDEN_SPEC_ENTRIES "")
foreach(spec_file ${GOLDEN_SPEC_FILES})
    get_filename_component(spec_name "${spec_file}" NAME)
    string(APPEND GOLDEN_SPEC_ENTRIES "    \"${spec_name}\",
")
endforeach()
list(LENGTH GOLDEN_SPEC_FILES GOLDEN_SPEC_COUNT)
file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/golden_specs.h"
"/* Generated by examples/c/CMakeLists.txt -- do not edit. */
#ifndef WICKRA_RADAR_GOLDEN_SPECS_H
#define WICKRA_RADAR_GOLDEN_SPECS_H
#include <stddef.h>
#define GOLDEN_DIR \"${GOLDEN_DIR}\"
static const char *GOLDEN_SPECS[] = {
${GOLDEN_SPEC_ENTRIES}    NULL
};
static const size_t GOLDEN_SPEC_COUNT = ${GOLDEN_SPEC_COUNT};
#endif
")

# The test, as opposed to the examples above: `scan` and `scanpp` print a
# report and exit zero. This one asserts golden parity and operating-mode
# equivalence over the whole corpus.
add_executable(golden_test golden_test.c)
target_include_directories(golden_test PRIVATE
    "${WICKRA_RADAR_INCLUDE}" "${CMAKE_CURRENT_BINARY_DIR}")
target_link_libraries(golden_test PRIVATE "${WICKRA_RADAR_LIB}")

# On Windows the runtime DLL must sit next to the executable (there is no rpath);
# without it the loader pops a modal "DLL not found" dialog that hangs ctest. Copy
# it next to each example after build. (Unix resolves the .so/.dylib via the
# linker's embedded path, so this is Windows-only.)
if(WIN32)
    foreach(example scan scanpp golden_test)
        add_custom_command(TARGET ${example} POST_BUILD
            COMMAND ${CMAKE_COMMAND} -E copy_if_different
                "${WICKRA_RADAR_LIB_DIR}/wickra_radar.dll"
                "$<TARGET_FILE_DIR:${example}>"
            VERBATIM)
    endforeach()
endif()

enable_testing()
add_test(NAME scan COMMAND scan)
add_test(NAME scanpp COMMAND scanpp)
add_test(NAME golden_test COMMAND golden_test)
# A missing runtime dependency must fail fast, never block the runner for hours.
set_tests_properties(scan scanpp golden_test PROPERTIES TIMEOUT 60)
